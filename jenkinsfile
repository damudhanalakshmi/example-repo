pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/your_username/example-repo.git'
            }
        }
        stage('Get Commit History') {
            steps {
                sh 'git log'
            }
        }
    }
}
